server:
  port: '3000'

spring:
  profiles:
    active: dev

---

spring:
  profiles: dev
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: student
    url: jdbc:mysql://localhost:3306/ashion
    password: student
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: 'true'

